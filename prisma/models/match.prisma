model Match {
  id         Int      @id @default(autoincrement())
  identifier String   @unique
  opponent   String
  location   String
  league     String
  date       DateTime
  emblem     String

  @@map("match")
}
